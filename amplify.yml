version: 1
frontend:
  phases:
    build:
      commands:
        - 'echo "Build started"'
    artifacts:
      baseDirectory: /
      files:
        - '**/*'
  cache:
    paths: []
  backend:
    phases:
      build:
        commands:
          - 'echo "Backend build started"'
    artifacts:
      baseDirectory: /
      files:
        - '**/*'
  cache:
    paths: 
